import AdminContentReviewClient from './AdminContentReviewClient';

export const metadata = {
  title: '内容审核 | WAGMI Admin',
  description: '审核待发布的内容',
};

export default function AdminContentReviewPage() {
  return <AdminContentReviewClient />;
}
